machine:
  node:
    version: 6.7.0

test:
  override:
    - mocha test --reporter mocha-junit-reporter --timeout 10000:
        environment:
          MOCHA_FILE: $CIRCLE_TEST_REPORTS/junit/test-results.xml
